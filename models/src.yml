version: 2

sources:
    - name: btc
      database: btc
      schema: BTC_SCHEMA
      tables:
        - name: btc
          freshness:
              warn_after: {count: 1, period: hour}
          loaded_at_field: BLOCK_TIMESTAMP